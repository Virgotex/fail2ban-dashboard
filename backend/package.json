{
  "name": "fail2ban-dashboard-agent",
  "version": "2.0.0",
  "description": "Fail2Ban Dashboard agent — API-only service that runs on each monitored server",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^4.21.1",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
